import { StyleSheet } from 'react-native';

const appStyles = StyleSheet.create({
  welcome: {
    fontSize: 20,
    textAlign: 'center',
    margin: 10,
  },
  menu: {
    margin:10,
    alignSelf:'stretch',
  }
});

export default appStyles;